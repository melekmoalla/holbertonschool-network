#!/usr/bin/env bash
#Write a Bash script that pings an IP address passed as an argument.


for ip in "$@"; do
    if ping -c 1 -q "$ip"
    then echo "L'address $ip ping"
    else Usage: 5-is_the_host_on_the_network {IP_ADDRESS}
    fi
done
